{
  "name": "iomars",
  "version": "2.1.1",
  "description": "Isle of Man Amateur Radio Society Website",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node build-news.js && node build-minutes.js && node build-certificates.js && node build-equipment.js && node build-links.js && node inject-solar.js && node build-commits.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^17.0.1",
    "xml2js": "^0.6.2"
  }
}
